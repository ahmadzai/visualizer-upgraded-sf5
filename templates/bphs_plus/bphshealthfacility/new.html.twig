{% extends 'control-template.html.twig' %}

{% block content %}
    <h1>Bphshealthfacility creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('bphs_hf_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
