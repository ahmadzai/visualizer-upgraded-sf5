{% extends 'template.html.twig' %}

{% block content %}
    <h1>Bphsindicatorreach</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bphsIndicatorReach.id }}</td>
            </tr>
            <tr>
                <th>Reportmonth</th>
                <td>{{ bphsIndicatorReach.reportMonth }}</td>
            </tr>
            <tr>
                <th>Reportyear</th>
                <td>{{ bphsIndicatorReach.reportYear }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if bphsIndicatorReach.createdAt %}{{ bphsIndicatorReach.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bphs_indicator_reach_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bphs_indicator_reach_edit', { 'id': bphsIndicatorReach.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
