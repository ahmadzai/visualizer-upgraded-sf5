{% extends 'base.html.twig' %}

{% block title %}BphsIndicatorReach{% endblock %}

{% block body %}
    <h1>BphsIndicatorReach</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bphs_indicator_reach.id }}</td>
            </tr>
            <tr>
                <th>Reach</th>
                <td>{{ bphs_indicator_reach.reach }}</td>
            </tr>
            <tr>
                <th>ReportMonth</th>
                <td>{{ bphs_indicator_reach.reportMonth }}</td>
            </tr>
            <tr>
                <th>ReportYear</th>
                <td>{{ bphs_indicator_reach.reportYear }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ bphs_indicator_reach.slug }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ bphs_indicator_reach.createdAt ? bphs_indicator_reach.createdAt|date('Y-m-d H:i:s T') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ bphs_indicator_reach.updatedAt ? bphs_indicator_reach.updatedAt|date('Y-m-d H:i:s T') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bphs_indicator_reach_index') }}">back to list</a>

    <a href="{{ path('bphs_indicator_reach_edit', {'id': bphs_indicator_reach.id}) }}">edit</a>

    {{ include('bphs_plus/indicator_reach/_delete_form.html.twig') }}
{% endblock %}
